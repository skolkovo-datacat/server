# Running distrubition

## Configuring
You can configure server using `.env` file

## Prerequisites:
1. `node` in path (LTS version)

## Running
1. Extract dist archive to any folder
1. Run `run.bat` or `run.ps1`